[package]
name = "demo"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
lurq = { path = "../lurq", default-features = false, features = ["winit", "wgpu", "dx12", "image", "svg", "resources", "devtools", "clipboard", "form", "router"] }
tracing.workspace = true
